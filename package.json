{
  "name": "@rewind/forge",
  "version": "1.0.0",
  "description": "The Forge — torrent/usenet indexers + debrid resolution for Rewind, packaged as an operator-installed addon.",
  "license": "UNLICENSED",
  "private": false,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./host": "./src/host.ts",
    "./types": "./src/types.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "esbuild src/plugin.ts --bundle --format=esm --platform=node --target=node22 --outfile=dist/index.mjs --tsconfig=tsconfig.json"
  },
  "dependencies": {
    "@viren070/parse-torrent-title": "^0.7.6"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "@types/node": "^25.8.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  }
}
